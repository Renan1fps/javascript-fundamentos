/**
 * A função concat irá pegar dois arrays e juntar em um só gerando um novo array, podendo também adicionar novos, passando no parametro
 */

const filhos = ["francisco", "lucas"];
const filhas = ["hinata", "isadora"];
const todos = filhas.concat(filhos);
console.log(todos);
